# VPN Profiles

Place your `.ovpn` files in `~/vpn/` on the installed system.

The polybar VPN module uses rofi to let you pick which profile to connect to.
Files are gitignored — never commit VPN credentials.
