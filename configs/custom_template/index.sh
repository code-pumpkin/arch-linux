#!/bin/bash
# Custom config deployment template
# See README.md for full documentation
#
# Variables provided by the installer:
#   $home       — user home directory (e.g., /home/alice)
#   $cfg        — user .config directory (e.g., /home/alice/.config)
#   $SRC        — path to this config directory (source files)
#   $username   — the created username
#   $KB_LAYOUT  — keyboard layout (e.g., us, de, fr)
#   $KB_VARIANT — keyboard variant (e.g., colemak_dh, dvorak, or empty)

# --- Add your deployment commands below ---

echo "Custom config template — replace this with your own deployment logic."
echo "See configs/custom_template/README.md for instructions."
